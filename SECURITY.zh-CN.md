<!-- generated-by: gsd-doc-writer -->
# 安全策略

[English](SECURITY.md)

## 支持版本

Mercury 目前处于 Alpha 开发阶段。安全修复应用于 `master` 的最新修订；当前不维护旧发布分支。v1 在 CPython 3.11+ 上的安全支持目标是 Windows 和 Ubuntu。

## 报告漏洞

不要为疑似漏洞创建公开 issue、discussion 或拉取请求。

请使用仓库的私密 [GitHub Security Advisory 报告表单](https://github.com/EngelsVon/mercury-network-diagnostics/security/advisories/new)，或打开 **Security** → **Advisories** → **Report a vulnerability**。如果私密漏洞报告不可用，请私下保留报告，直到维护者公布其他正式私密渠道。本仓库没有公布安全邮箱；请勿猜测邮箱，也不要把敏感信息发送给无关联系人。

只需提供复现和评估问题所必需的信息：

- 受影响的修订或版本、平台和 Python 版本；
- 漏洞类别及受影响的信任边界；
- 仅使用回环地址，或你拥有/已获明确授权的私有网络的最小复现步骤；
- 预期与实际行为、影响，以及已知时的缓解建议；
- 已删除令牌、私钥、证书、地址、主机名、负载和凭据的脱敏日志或证据。

不要附带有效凭据或生产私钥。不要针对公网目标、第三方系统、项目维护者基础设施，或任何你不拥有且未获明确许可的网络进行测试。不要把概念验证扩大到证明问题所需的最小范围之外，不得中断服务、留存私有数据或尝试凭据暴力破解。

维护者应确认收到私密报告，在受控环境中复现，协调修复与披露时间，并在报告者要求时致谢。项目目前没有公布响应时限服务等级。

## 安全边界

涉及安全的贡献必须保留以下仓库保证：

- 主动目标仅限回环、RFC1918 IPv4、IPv6 ULA 或带作用域的 IPv6 链路本地地址/CIDR，并按需要求明确授权。公网、文档保留、多播、未指定、广播及逃逸范围的 DNS 结果必须在 I/O 前失败。
- 所有主动工作都经过统一策略和不可变的主机、端口、尝试、逻辑字节、速率、并发、时长、事件及输出上限。
- 非回环 Web 监听器需要 TLS 和令牌；对等控制还需要双向 TLS、已配置的证书信任/固定、令牌与重放检查，以及固定配置的目标。
- 对等控制不能成为第三方扫描中继；可选 Nmap 适配器只能接受由计划派生的固定配置，不能接受任意参数、脚本、目标文件、代理、诱饵或负载。
- 历史记录和报告拒绝凭据、令牌与私钥；标识符和负载默认脱敏。
- 静默或超时是不确定证据。有限测试矩阵无法证明所有隧道或报文序列均被阻断。

`tests/fixtures/tls/` 中的证书和密钥仅是提交到仓库的测试夹具，绝不能在生产环境中信任或部署。

## 安全研究范围

Mercury 只用于操作者自有私有网络，或已获得明确授权的私有网络中的受控诊断。私有 IP 地址本身不代表拥有权限。协同安全研究必须严格限制在资产所有者许可的系统、端口、配置、速率、时长和数据处理范围内。
