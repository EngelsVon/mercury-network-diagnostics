---
phase: 01-evidence-and-safety-foundation
plan: "03"
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - src/mercury/cli.py
  - src/mercury/render.py
  - tests/test_cli.py
  - tests/test_contracts.py
  - README.md
autonomous: true
requirements: [TEST-01]
user_setup: []
must_haves:
  truths:
    - "The console script and `python -m mercury` use one argparse implementation and expose version/model/plan/history commands."
    - "Human output and JSON are projections of the same typed values, with structured errors and stable exit codes."
    - "A developer can run one offline synthetic task, observe progress, cancel it, and inspect its persisted partial result."
    - "The full standard-library suite covers every Phase 1 requirement without sending public network traffic."
  artifacts:
    - path: src/mercury/cli.py
      provides: "Single Phase 1 command surface and exit-code mapping"
    - path: src/mercury/render.py
      provides: "Human projections of canonical models"
    - path: tests/test_cli.py
      provides: "Subprocess/argument/JSON/error/exit-code tests"
    - path: tests/test_contracts.py
      provides: "Cross-module Phase 1 acceptance and secret/cardinality tables"
    - path: README.md
      provides: "Honest Phase 1 developer quick start and safety semantics"
  key_links:
    - from: src/mercury/cli.py
      to: src/mercury/tasks.py
      via: "CLI delegates task work to TaskService"
    - from: src/mercury/cli.py
      to: src/mercury/render.py
      via: "Both JSON and human paths project canonical values"
    - from: pyproject.toml
      to: src/mercury/cli.py
      via: "Console entry point calls the same main function as __main__"
---

<objective>
Expose the Phase 1 foundation through one coherent CLI and close its acceptance
test surface.

Purpose: The foundation is useful and verifiable only if later phases have a
stable entry point, projections, errors, and runnable safety regression suite.
Output: CLI/rendering, offline lifecycle commands, integration contracts, and
the initial developer documentation.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-evidence-and-safety-foundation/01-CONTEXT.md
@.planning/phases/01-evidence-and-safety-foundation/01-01-SUMMARY.md
@.planning/phases/01-evidence-and-safety-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: One CLI and two faithful projections</name>
  <files>src/mercury/cli.py, src/mercury/render.py, tests/test_cli.py</files>
  <action>
Build argparse commands for `version`, `model`, `plan`, `history list/show`,
and `task synthetic`. Keep the synthetic command clearly marked offline/
developer. Make global `--json`, data-path, and verbosity behavior predictable.
Render human output from the same canonical dict/result used by JSON. Define
documented exit codes for success, partial/cancelled, policy rejection, invalid
input, and internal error. Never echo credential-shaped values.
  </action>
  <verify>
Run `python -m unittest tests.test_cli -v`; run
`python -m mercury model --json`; run `python -m mercury --help`.
  </verify>
  <done>
Module and installed entry points behave identically and scripts can depend on
stable JSON/exit semantics.
  </done>
</task>

<task type="auto">
  <name>Task 2: Phase-wide acceptance contracts</name>
  <files>tests/test_contracts.py</files>
  <action>
Add table-driven cross-module tests for all observation dispositions/evidence
kinds/confidences, scoped-link-local parsing, scope/DNS re-check, every logical
absolute budget dimension, confirmation
gates, legal/illegal task transitions, cancellation persistence, history
retention, and credential rejection. Patch resolvers/clocks/runners; do not
open public sockets. Include a regression proving UDP/ICMP-style silence stays
inconclusive through model→history→CLI JSON.
  </action>
  <verify>
Run `python -m unittest discover -s tests -v`.
  </verify>
  <done>
All Phase 1 requirements have a runnable regression and the suite is
deterministic/offline.
  </done>
</task>

<task type="auto">
  <name>Task 3: Clean installation and foundation documentation</name>
  <files>README.md, pyproject.toml</files>
  <action>
Document the NARROW-GO scope, evidence semantics, authorization and absolute
budget philosophy, offline Phase 1 commands, and explicit non-goals. Verify
package metadata and included modules from a built wheel in a temporary virtual
environment when the local build tooling is available; otherwise use
`pip wheel --no-deps` and record the exact fallback. Do not claim network
features scheduled for later phases.
  </action>
  <verify>
Run `python -m pip wheel . --no-deps -w dist` and invoke the wheel's CLI from a
temporary virtual environment.
  </verify>
  <done>
A clean interpreter can install the wheel and run help/version/model; README
does not overstate unfinished functionality.
  </done>
</task>

</tasks>

<verification>
Run `python -m unittest discover -s tests -v`, `python -m mercury --help`, and
the clean-wheel smoke command.
</verification>

<success_criteria>
- One CLI exposes the canonical model, policy preview, history, and synthetic
  lifecycle without duplicate behavior.
- All Phase 1 requirements have deterministic tests.
- A clean wheel install works and documentation states only shipped behavior.
</success_criteria>

<output>
After completion, create `01-03-SUMMARY.md`.
</output>
