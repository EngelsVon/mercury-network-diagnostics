---
phase: 5
plan: 01
type: execute
wave: 1
depends_on:
  - 01-01
  - 02-01
  - 03-01
  - 04-01
files_modified:
  - README.md
  - src/mercury/cli.py
  - src/mercury/web/static/index.html
  - tests/test_contracts.py
  - tests/test_installation.py
  - tests/test_cli.py
  - tests/test_web.py
  - tests/test_paired.py
  - tests/test_nmap_adapter.py
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
  - QUAL-03
  - DOC-01
---

<objective>
Prove the shipped private-network coverage contract with controlled tests,
document its actual limits and operator procedure, and close the planning
milestone only after the implementation is verified.

Purpose: isolation-leak results are useful only when the exact packet/profile
coverage, receiver prerequisite, confidence and gaps are understandable and the
software can be released without testing against an unowned host.

Output: complete controlled verification, release/installation smoke checks,
accurate CLI/Web/README guidance and synchronized project state.
</objective>

<threat_model>
Assets: truthful operator conclusions, private endpoint protection, evidence
reproducibility, build artifacts and release guidance.

Threats and mitigations:

- Documentation or UI promises universal tunnel detection.  Review all user
  text against TUNNEL-COVERAGE.md and require scoped-matrix wording.
- Tests accidentally scan a real peer/private network.  Use loopback, fake
  sockets/native runners/XML fixtures and a test-level guard that rejects the
  supplied endpoint and non-loopback I/O.
- A compatibility/build path omits new model evidence.  Run package, install,
  import, CLI help and serialization fixture checks on the supported interpreter.
- A requirement is marked complete without evidence.  Update planning state only
  after the recorded acceptance commands pass and link each requirement to tests.
</threat_model>

<tasks>

<task type="tdd">
  <name>Task 1: Complete contract and adversarial controlled coverage tests</name>
  <files>tests/test_contracts.py, tests/test_paired.py, tests/test_nmap_adapter.py, tests/test_cli.py, tests/test_web.py</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/TUNNEL-COVERAGE.md
    - tests/test_contracts.py
    - tests/test_paired.py
    - tests/test_nmap_adapter.py
  </read_first>
  <action>
    Add a compact requirement-to-test contract table and controlled adversarial
    tests for: private/public/DNS-escape rejection before I/O; CIDR canonical
    aggregation; all hard ceilings and zero-duration semantics; cancellation;
    every receiver profile/direction; malformed/expired source-mismatched tags;
    ICMP capability gaps; cross-subnet ARP/ND non-applicability; Nmap argv/XML
    constraints; CLI/Web facade routing; peer trust/replay; history redaction.
    Use loopback, fakes and fixtures only.  Add a test helper/guard that fails
    immediately if a test attempts the supplied 172.27.20.195 endpoint or any
    real non-loopback active connection.
  </action>
  <acceptance_criteria>
    - Every v1 QUAL/COVER/MAP/NMAP/SURF/PEER/HIST/SCOPE requirement has a named
      controlled test or a documented capability-gated test path.
    - Tests distinguish direct carrier evidence from timeout/silent/gap output;
      no assertion accepts a universal no-tunnel claim.
    - The test guard proves the suite has no real non-loopback probe dependency.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Write precise internal assessment and receiver deployment guidance</name>
  <files>README.md, src/mercury/cli.py, src/mercury/web/static/index.html</files>
  <read_first>
    - README.md
    - .planning/TUNNEL-COVERAGE.md
    - .planning/PROJECT.md
    - src/mercury/cli.py
    - src/mercury/web/static/index.html
  </read_first>
  <action>
    Replace obsolete general-diagnosis/public examples with private/loopback-safe
    commands.  Document how to install/configure the two Mercury peers with
    mTLS/token/pins and preconfigured receiver profile/port/certificate values;
    then show how to run an attested two-direction assessment, a multi-CIDR map
    and an optional fixed Nmap profile.  Explain rate/concurrency/logical
    attempt-start accounting, hard ceilings, duration `0`, cancellation and
    redaction.

    Include the actual supported coverage matrix: TCP, UDP, DNS UDP/TCP, ICMP
    echo, TLS, HTTP, SSH banner, local ARP/ND and optional Nmap TCP/SYN/UDP/SCTP.
    State exactly what direct positive receiver evidence proves (the selected
    carrier could convey a message during this recorded test) and what a negative
    result means (only that matrix); ICMP observer and ARP/ND scope limitations
    must be prominent.  Never include an instruction to use raw payloads,
    credentials, public targets or arbitrary Nmap flags.
  </action>
  <acceptance_criteria>
    - README/CLI/Web copy has no successful public-target example or universal
      “all tunnels eliminated” claim.
    - Guidance lists receiver prerequisites and every supported/gap outcome,
      including cross-subnet ARP/ND non-applicability.
    - Help/HTML tests confirm rendered user language matches the matrix contract.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: Run release verification and synchronize planning evidence</name>
  <files>tests/test_installation.py, .planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - AGENTS.md
    - pyproject.toml
    - tests/test_installation.py
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
  </read_first>
  <action>
    Run the full controlled test suite, compile source/tests, build the package,
    install the resulting distribution into an isolated temporary environment,
    run `python -m mercury --help` and safe loopback smoke commands, and validate
    result codec compatibility fixtures.  Do not contact configured real peers
    as part of verification.  Fix only defects within this roadmap and rerun the
    affected/full checks until green.

    Mark requirements validated only after their behaviour/tests pass, update the
    roadmap plan counts and phase status, record exact verification commands and
    results in STATE, and refresh PROJECT decisions/context to match the shipped
    product.  Keep v2 items pending rather than implying future comparison/import
    work was delivered.
  </action>
  <acceptance_criteria>
    - `python -m unittest discover -s tests -v`, `python -m compileall -q src tests`, and `python -m build` exit 0.
    - Isolated install/import/help smoke succeeds on the available Python 3.13 interpreter.
    - Planning documents mark only evidence-backed v1 requirements complete and retain the finite-coverage limitation.
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. Execute all focused requirement tests and the full controlled suite without a real non-loopback connection.
2. Compile and build, then perform isolated installation/import/CLI-help smoke verification.
3. Review every README/help/Web conclusion string against `.planning/TUNNEL-COVERAGE.md`.
4. Verify project state contains the exact command results before marking the milestone complete.
</verification>

<success_criteria>
- The released product has controlled evidence for every v1 requirement and safe installation/build checks.
- Operators can set up paired receivers and understand exactly which protocol/packet-shape coverage was exercised.
- Positive results identify candidate carriers; negative results visibly retain their finite scope and gaps.
- Planning documents accurately reflect the verified implementation and remaining v2 work.
</success_criteria>

---

*Plan created: 2026-08-02*
